<div class="container">
  <div class="row gx-5">
    <div class="col">
      <div class="card w-100 mb-3">
        <div class="card-body">


<h1>施設の新規登録</h1>

<%= render 'form', room: @room %>

<%= link_to 'Back', rooms_path %>
